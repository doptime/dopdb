# macOS
.DS_Store

# Go
bin/
*.test
*.out
coverage.txt
cover.profile

# TypeScript / Node
node_modules/

# Editors
*.sw[pa]
*~

# files to ignore
*.zip
*.tar.gz
*.toml
**/dist/
# Round-tracking scratch: the tracked round docs live at workrounds/Round*/,
# but the per-round `files/` working copies are stale source snapshots that
# break `go build ./...` if committed. Never track them.
workrounds/**/files/
_packs/**